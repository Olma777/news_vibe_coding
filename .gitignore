# Секреты и локальное состояние
.env
.env.*
!.env.example
.claude/

# Рабочие выходные дайджесты (эфемерные). Только top-level — docs/digests/ публикуется в архив.
/digests/

# macOS / редакторы
.DS_Store
*.swp
